# Kafka 중복·유실·지연 설계와 메시지 전송 보장 방식 적용 검토

---

## 1. 메시지 전송 보장 방식 정리

### 1-1. At most once ( 중복 X , acks = 0 - 응답을 기다리지 않음)

메시지를 최대 한 번만 처리하는 방식.

Consumer가 메시지를 읽고 offset을 먼저 commit한 뒤 실제 DB 반영 전에 애플리케이션이 죽으면, Kafka 입장에서는 이미 처리된 메시지로 보기 때문에 재처리되지 않습니다.

중복 가능성은 낮지만 장애 상황에서 유실이 발생할 수 있습니다. 로그 수집, 모니터링 이벤트처럼 일부 데이터가 빠져도 전체 의미가 크게 깨지지 않는 곳에는 사용할 수 있지만, 결재 상태 변경처럼 반드시 처리되어야 하는 업무에는 적합하지 않습니다.

### 1-2. At least once (중복 허용 , retry 함, (acks 1, all))

메시지를 적어도 한 번 이상 처리하는 방식.

Consumer가 DB 반영은 성공했지만 offset commit 전에 장애가 나면 같은 메시지를 다시 읽어 중복 처리할 수 있습니다. 유실 가능성은 줄어드는 대신 중복 처리를 전제로 하기 때문에, Consumer 로직에 멱등성 설계가 반드시 필요합니다.


### 1-3. Exactly once (중복 X , retry 를 하지만 중복이 제거됨)

이름만 보면 메시지가 물리적으로 정확히 한 번만 전달되는 것처럼 보이지만, 실무적으로는 **최종 비즈니스 결과가 한 번만 반영되도록 만드는 것**에 가깝다고 이해함.

기본적으로 멱등성이 보장이 되어야 함.

Kafka의 idempotent producer나 transaction은 특정 구간의 보장을 강화할 수 있지만, Consumer가 DB를 업데이트하거나 메일/알림을 발송하는 과정까지 자동으로 보장해주지 않음.

따라서 exactly once를 말하려면 어떤 경계 안에서의 보장인지를 명확히 해야함.

- Kafka 내부(Producer → Broker)에서 정확히 한 번인지 (3.0 이후 부터는 기본값이 enable.idempotence = true, 프로듀서는 메시지를 보낼 때  Producer ID 와 sequnce number 를 헤더에 담음. 브로커는 Producer 가 보낸 메시지의 sequence 가 브로커가 가지고 있는 메시지의 sequnce 보다 1만큼 큰 경우에만 브로커에 저장, 이미 가지고 있는 시퀀스의 경우 메시지 로그를 저장하지 않고 ack 만 보냄)
- DB 반영까지 포함하는지
- 알림/메일 같은 외부 side effect까지 포함하는지

---

## 2. 내 서비스에 적용할 흐름과 판단

### at most once : 중복 X, 유실 가능 

이메일 회사:
firebase모바일 알림 (알림 유실 가능)

보안회사 : 
(after commit 후 저장)
진단 종료 후 자동 조치 요청을 만드는 후처리 

(after commit 후 저장)
감사 로그 기록

### at least once : (중복이 허용되는 겅우)

이메일 회사: 
smtp 메일 발송 큐
smtp 전송 처리에서는 결과가 tempory_failure 이고 재시도가 가능한 경우 result_error 를 반환 , 큐 프로세서는 retry 횟수와 다음 retry delay 를 기록한 후 메시지를 다시 재처리
영구 실패하거나, 재시도 한도를 초과한경우 실패안내 메일을 보내거나 failed 디렉터리(카프카의 DLQ 기능과 유사)로 이동 


보안 회사:
결제 후 알림 발송 
중복이 허용되는 상대측 메일 서버 응답이 늦게와 timeout 이 발생하여 실패로 인지함.
재시도 로직이 실행됨.
동일 알림이 5번 이상 가는 현상 발생

timeout 설정을 늘려서 해결. 



### exactly once 

이메일 회사

검색 인덱스 
insert/update/delete
mailUid 기반으로 처리되어 멱등함. 멱등은 하지만 정확히 한번 동작한다? exactly once 사례인지는 부정확

---


---

## 3. Retry / DLQ / Idempotency 기준

### 3-1. Retry 대상

일시적인 오류는 Retry 대상.

- DB 커넥션 일시 오류
- 네트워크 timeout
- 외부 API 일시 장애
- 일시적인 lock 경합

무한 재시도를 방지하기 위해 재시도 횟수를 제한하고, exponential backoff + jitter를 적용해 재시도 폭풍을 방지해야 함.
어떤 경우에는 이진 백오프를 적용하고 어떤 경우는 이진 백오프가 아닌 일정 시간 이후 재시로를 적용하는지 궁금합니다!

### 3-2. DLQ 대상

계속 재시도해도 성공 가능성이 낮은 메시지는 DLQ로 분리

- 필수 값 누락
- 이미 삭제된 대상에 대한 이벤트
- 비즈니스 상태상 처리 불가능한 이벤트

DLQ로 분리 후 운영자가 원인을 확인하고 데이터 보정 또는 수동 재처리를 해야 합니다. 실패 이벤트에는 아래 정보가 남아야 추적이 가능합니다.

