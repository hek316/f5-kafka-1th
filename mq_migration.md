# 메일 발송 파이프라인의 메시지 브로커 전환 검토

> 목적: 현재 파일 큐 기반의 메일 발송/검색 처리 구조를 RabbitMQ, 또는 카프카 전환하는 것에 대한 적절성을 검토
>
>  관점: 전환의 이점, 비용, 순서 보장 필요성을 따져 의사결정 근거를 정리

---

## 1. 현재 아키텍처 요약 (As-Is)

"메일 발송"과 "검색 인덱싱"은 같은 줄기가 아니다. 인덱싱은 발송 시점이 아니라, 메일이 **사용자 메일함에 저장(배달)되는 시점**에 붙는다.

```text
■ 메일 수신·배달  (SMTP/MDA 서버)
   들어온 메일 (내부 발신 포함)
      ▼
   메일 처리 파이프라인  ── 로컬 디스크 파일 큐
      ▼  여러 처리 단계 통과
   메일함에 저장 (DB insert + 본문 파일) → 트랜잭션 커밋
      ▼  커밋 직후, "대량메일이 아니면"
   색인 요청  ── 비동기(@Async, 논블로킹) ── TCP 소켓 ──┐
                                                          │
■ 인덱서 서버  (별도 프로세스, 예: :8878)                 │
   소켓 수신  ◀───────────────────────────────────────────┘
      ▼
   내부 큐 적재 (인덱서 서버 자체 로컬 파일 큐 = 내구성 버퍼)
      ▼
   Lucene 색인 → 성공 시 버퍼 파일 삭제
      ▼
   인덱스 파일 볼륨에 "사용자별 디렉터리"로 영구 저장
   {인덱스볼륨}/도메인/아이디/  (Lucene 세그먼트 파일)
   ※ 멀티 노드면 전용/공유 스토리지에 위치

```

핵심 특징 3가지

1. 메일 처리(수신·배달·발송)는 **로컬 디스크 파일 큐** 기반임. 별도 브로커 없이 각 프로세스가 자기 로컬 디스크(`{cts.home}/queue/...`)에 파일로 큐를 쌓아 처리
  1. 성공 시 파일 삭제, 실패 시 재시도하다 한도를 넘으면 실패(failed) 보관함으로 옮김
2. 검색 인덱싱은 메일이 메일함에 저장되는 시점에 트리거됨.
  1. 저장·커밋 직후 **비동기**로 색인 요청을 보냄
3. 검색 인덱싱은 **별도 인덱서 서버**가 담당하고, 통신은 파일이 아니라 **TCP 소켓**으로 이뤄짐.

### 1-1. 현재 구조의 장점과 한계

**장점**

- 외부 미들웨어 의존성이 없어 설치, 운영이 단순
- 큐가 로컬 디스트에 남으므로 재시작 후에도 보존
- 검색 인덱서가 별도 네트워크 서비스라, 인덱서가 죽어도 발송 자체는 계속됨.

**한계 (전환을 고민하는 이유)**

- **메일 처리 큐가 받은 서버의 로컬 디스크에만 쌓임.**
  - 한 서버가 받아 큐에 넣은 메일은 그 서버만 처리할 수 있음,
  - 그래서 그 서버가 밀리거나 죽어도 다른 서버가 대신 가져가 처리하지 못하ㅁ(밀린 작업 재분배·장애 시 인계 불가). 들어오는 연결은 로드밸런서(VIP)로 분산할 수 있지만, **이미 쌓인 큐는 공유되지 않아** 부하가 자동으로 나뉘지 않는다. 결국 일꾼(워커)을 늘려 부하를 나누는 수평 확장이 구조적으로 어려움.
- **색인 요청 경계에 유실 가능성,** 색인은 비동기 fire-and-forget 소켓 호출이라, 보내는 순간 인덱서가 닿지 않으면 요청이 유실될 수 있음.(현재는 메일에 '색인 여부' 플래그를 두고 재색인 스케줄러로 누락분을 보완).
- **적체/지연 가시성이 약함.** 표준 지표(소비 지연 등)가 없어 큐에 얼마나 쌓였는지를 직접 확인해야 함.
- **실패 디렉토리에 있는 조회, 재처리하는 하는 부분 수작업 필요.**

---

## 2. 전환 범위 정의

전환 대상은 **메일 한 건에 딸려 일어나는 비동기 곁다리 작업**(외부 발송, 검색 색인, 모바일 알림(FCM), 저널링/아카이빙, 통계·로그)이다. 이들을 외부 브로커(RabbitMQ 또는 Kafka)로 통합한다.

목표 구조는 **"하나의 메일 이벤트를 브로커가 여러 소비자(발송 · 색인 · 알림 · 아카이빙 · 통계)에게 fan-out 하는 것"** 이다. 메일 한 건이 생기면 그걸 받아 처리할 곳마다 복사돼 각자 독립적으로 일하고, 새 소비자(예: 비활성 상태인 ES 아카이빙 검색)도 쉽게 붙일 수 있다.

지금은 처리 방식이 제각각(발송=로컬 파일 큐, 색인=비동기 소켓, 알림=단발 호출)이라 **색인·알림은 유실 위험이 있고, 얼마나 밀렸는지도 보기 어렵다**(상세 한계는 1-1 참고). 브로커로 통합하면 ① 메시지를 안전하게 받아두는 **내구성 버퍼**, ② 실패 시 자동 재시도와 실패 보관(**DLQ**), ③ 얼마나 밀렸는지 보여주는 **적체 지표**가 기본으로 갖춰지고, 소비자별 독립 확장과 새 소비자 추가도 쉬워진다.

---

## 3. RabbitMQ vs Kafka 비교

두 제품은 "메시지를 중계한다"는 점만 같고 철학이 다르다.


| 관점          | RabbitMQ                                      | Kafka                                         |
| ----------- | --------------------------------------------- | --------------------------------------------- |
| 기본 모델       | 메시지 브로커. 큐에서 꺼내 ack하면 사라짐                     | 분산 로그(append-only). 읽어도 보존기간 동안 남음            |
| 라우팅/팬아웃     | Exchange(fanout/topic 등)로 유연한 라우팅, 팬아웃이 1급 기능 | Topic/Partition. 소비는 Consumer Group으로 분리해 팬아웃 |
| 순서 보장       | 단일 큐·단일 컨슈머에서 유지. 경쟁 컨슈머 늘리면 흐트러짐             | 파티션 단위 보장. 같은 key는 같은 파티션 → 키 단위 순서           |
| 처리량         | 중간~높음 (건당 라우팅·ack 비용)                         | 매우 높음 (대용량 스트리밍에 최적)                          |
| 재처리(replay) | ack되면 끝. 과거 메시지 재독 어려움                        | offset 되감아 과거 메시지 재처리 가능                      |
| 지연/재시도/DLQ  | TTL, dead-letter exchange 등 내장 풍부             | 기본 미지원, 별도 패턴 필요(retry/dead-letter topic)     |
| 적체 지표       | 큐 깊이(ready/unacked)                           | Consumer Lag(오프셋 격차)                          |
| 운영 난이도      | 상대적으로 단순                                      | 파티션 설계 등 학습·운영 비용 큼                           |
| 적합 시나리오     | 작업 큐 + 팬아웃 + 재시도/DLQ                          | 이벤트 로그 + 다수 소비자 + 재처리/대용량                     |


### 3-1. 판단의 분기점: "작업 큐"인가 "이벤트 스트림"인가

- 검색 인덱싱과 SMTP 발송은 **"한 번 처리하면 끝나는 작업(task)"** 성격이 강하다. 처리 후 보존 필요가 적고, 실패 시 재시도·DLQ가 중요하다. → **RabbitMQ의 작업 큐 모델에 자연스럽게 맞는다.**
- 반대로 발송 이벤트를 로그로 남겨 **재처리(replay)** 하거나, 소비자 종류가 계속 늘거나, **대량 발송 처리량**이 1차 목표라면 → **Kafka가 유리하다.**

(어느 쪽을 택할지에 대한 권고는 7장에 정리한다.)

### 3-2. (개념 메모) "키로 나눠 처리한다" — 지금 방식과 Kafka

일이 몰릴 때 "번호표를 여러 창구에 나눠 보내는" 방식이다. 나누는 흔한 방법이 "키 % N"인데, **지금 시스템과 Kafka가 같은 원리를 쓴다.**

- **지금(색인)**: `사용자번호 % 3` → 0/1/2번 서버에 나눠 색인. 같은 사용자는 늘 같은 서버.
- **Kafka**: `키 % 파티션수` → 파티션에 나눠 담고 파티션마다 컨슈머가 처리. 같은 키는 늘 같은 파티션.

다만 (1) 지금 이 분배는 **색인에만** 있고 발송은 안 나뉘는데, 브로커를 쓰면 **발송도** 워커를 늘려 나눌 수 있다(예: 공지 5천 통을 발송 워커 3대가 분담). (2) 지금은 서버를 늘리면 `% 3`→`% 4`로 분배가 어긋나 손으로 다시 맞춰야 하지만, Kafka는 **자동 재분배**한다.

---

## 4. Before / After 아키텍처

### 4-1. Before (As-Is)

```text
[발송] ─▶ [SMTP 서버] ─▶ 로컬 파일 큐(발송/배달) ─▶ 외부 SMTP
              │
              └─ 색인 요청 ─(TCP 소켓)─▶ [인덱서 서버] ─▶ 검색 인덱스(Lucene)
   특징: 발송은 프로세스 로컬 디스크 큐, 검색은 비동기 소켓 호출.
        경계에 내구성 완충/표준 재시도·DLQ·적체 지표가 없음.
```

### 4-2. After — 안 (A) RabbitMQ

```text
[발송] ─publish─▶  RabbitMQ
                    Exchange: mail.events (fanout/topic)
                       ├─▶ Queue: q.smtp   (+DLX) ─▶ [SMTP 워커 N]
                       └─▶ Queue: q.index  (+DLX) ─▶ [인덱서 워커 M]
   - 발송 이벤트를 exchange가 두 큐로 팬아웃.
   - 재시도/실패는 TTL + Dead-Letter Exchange로 표준화.
   - 네트워크 통신 → 서버 물리적 분리·증설 가능.
```

### 4-3. After — 안 (B) Kafka

```text
[발송] ─produce(key=mailId)─▶  Kafka Topic: mail.events (P 파티션, 로그 보존)
                                     │                  │
                          Consumer Group: smtp   Consumer Group: index
                                     │                  │
                              [SMTP 워커 N]        [인덱서 워커 M]
   - 한 Topic을 두 Consumer Group이 독립 소비(팬아웃).
   - key=mailId → 같은 메일 이벤트는 같은 파티션 → 키 단위 순서 보장.
   - 장애/재색인 시 offset 되감아 replay 가능.
```

> 두 안의 공통 핵심은 **"검색과 SMTP를 네트워크로 통신하는 독립 소비자로 분리"** 한다는 점이다. 이것이 Before와의 본질적 차이이자 전환의 실익이다.

---

## 5. 전환의 이점과 비용

### 5-1. 이점

1. **내구성 완충 + 장애 인계.** 가장 큰 실익. 생산자와 소비자 사이에 브로커가 들어가, 큐가 특정 서버 디스크에 묶이지 않는다. 그래서 ① 소비자가 죽어도 메시지가 브로커에 남아 다른 워커가 이어받고(지금은 색인 요청이 유실되거나 발송이 그 서버에 묶임), ② 평상시에도 워커를 다른 호스트로 자유롭게 늘려 부하를 나눌 수 있다.
2. **소비자 독립 확장.** 부하가 몰리는 역할(예: 발송)만 워커를 독립적으로 늘릴 수 있다.
3. **표준화된 재시도/DLQ.** RabbitMQ의 DLX, Kafka의 retry/dead-letter topic으로 수작업 재처리를 대체한다.
4. **운영 가시성.** 적체(Consumer Lag)를 표준 지표로 제공해, 모니터링 도구로 그래프·알림을 걸기 쉬워진다(지금은 큐 파일 개수를 직접 세어봐야 한다).
5. **(Kafka 한정) 재처리/감사.** 이벤트가 로그로 남아 인덱스 손상·신규 소비자 추가 시 과거 발송을 다시 흘려보낼 수 있다.
6. **백프레셔.** 생산이 소비를 앞질러도 브로커가 완충해 폭주 시 시스템을 보호한다.

### 5-2. 비용

1. **운영 복잡도 증가.** 브로커가 모든 메일 작업이 거쳐 가는 길목이 되므로, 브로커가 죽으면 발송·색인·알림이 전부 멈춘다(새로운 단일 장애점). 그래서 브로커 자체를 HA(고가용성: 한 대가 죽어도 다른 대가 이어받게 여러 대로 구성)로 운영해야 하고, 여기에 클러스터·디스크·모니터링·백업 관리 부담이 따라온다.
2. **컨슈머** **멱등성 설계**가 필요
3. **정합성 경계 이동.** "DB 상태 변경"과 "메시지 발행"을 원자적으로 묶기 어려워져 **Transactional Outbox 패턴**이 사실상 필요해진다.

---

## 6. 순서 보장(Ordering)이 필요한가?

결론: **전역 순서(global order)는 필요 없고, "같은 대상"에 대한 부분 순서(per-key order)는 필요하다.**

### 6-1. 전역 순서가 필요 없는 이유

서로 다른 메일의 발송·인덱싱은 순서가 바뀌어도 결과가 같다. 전역 순서를 강제하면 단일 컨슈머/단일 파티션으로 묶여 **확장성을 스스로 포기**하게 되어 전환 목적과 충돌한다.

### 6-2. 순서가 중요한 지점 (per-key)

**같은 대상에 대해 서로 반대되거나 의존하는 이벤트**가 있을 때만 순서가 중요하다.

- 검색 인덱스: 같은 메일의 `insert` → `delete/update` 가 뒤바뀌면 지워져야 할 문서가 남는다.
- 결재/승인 메일: `승인` → `반려` 같은 상태 전이가 뒤집히면 최종 상태가 틀어진다.

### 6-3. 해결 방법

- **Kafka:** message key를 대상 식별자(예: mailId)로 지정하면 같은 key는 같은 파티션으로 가 **대상별 순서가 보장**되고, 다른 대상은 다른 파티션으로 병렬 처리되어 확장성도 지킨다.
- **RabbitMQ:** 단일 큐는 순서를 유지하지만 경쟁 컨슈머가 붙으면 깨진다. 대상별 순서가 필요하면 consistent-hash exchange로 같은 key를 같은 큐로 보내고 그 큐를 단일 컨슈머로 두는 패턴을 쓴다(Kafka보다 손이 더 간다).

### 6-4. 순서만으로 부족하다 — 멱등성과 한 쌍

같은 파티션으로 순서를 보장해도, **재시도로 인한 중복**과 **오래된 이벤트가 최신 상태를 덮어쓰는 문제**는 따로 막아야 한다.

- 검색: 단순 추가(add)가 아니라 대상 키 기준 **upsert(update)** 로 재처리해도 결과가 같게 만든다.
- 상태 변경: `UPDATE ... WHERE id=? AND status=기대값` 같은 **조건부 갱신**으로 한 번만 전이되게 하고, 이벤트에 version/timestamp를 실어 **더 오래된 이벤트는 무시**한다.

즉 **순서 보장(브로커 레벨) + 멱등성/조건부 처리(소비자 레벨)** 가 함께 가야 안전하다.

---

## 7. 권고

### 7-1. 권고

- 현재 요구만 보면 **RabbitMQ를 1차 후보**로 본다(작업 큐 + 팬아웃 + DLQ 내장으로 마이그레이션이 자연스럽다).
- **재처리/대용량/멀티 컨슈머** 요구가 확실하면 **Kafka**로 간다.
- 순서는 **per-key(대상 식별자 기준)만** 보장하고, 소비자 멱등성을 반드시 함께 구현한다.
- DB 상태 변경과 메시지 발행이 함께 일어나는 구간은 **Transactional Outbox**를 도입한다.

---

## 8. 멘토님께 질문

1. RabbitMQ로 충분한 규모와 Kafka가 필요한 규모를 가르는 일반적인 기준선은 무엇인가요?
2. SMTP처럼 idempotency key를 지원하지 않는 외부 시스템에서, 중복 발송을 어디까지 허용하고 불확실 상태를 어떻게 운영하나요?
3. 검색 인덱스 재구축(replay)이 실제 운영 요구로 존재한다면, 그것만으로 Kafka 선택이 정당화되나요?_재색인 스케줄러를 따로 만들어서 불필요할것 같기도 합니다..

